# Frontend API Configuration
# Default to same-origin API path so deployed builds do not hardcode localhost.
# Vite dev server proxies /api to http://localhost:4000 (see vite.config.ts).
# Override with absolute URL only when frontend and API are hosted on different domains.
VITE_API_URL=/api
